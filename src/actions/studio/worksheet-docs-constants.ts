// Plain (non-"use server") shared constants for 학습지 조판(E21 · E22).
// 관용구 출처: src/actions/workbench/passage-constants.ts:1-3 —
// **"use server" 모듈은 async 함수만 export 할 수 있다.** 공유 계약은 이 상수를
// `src/actions/studio/worksheet-docs.ts` 에서 export 하도록 적었지만, 그 파일은
// 서버 액션 모듈("use server")이라 상수를 export 하는 순간 빌드가 깨진다
// ("Only async functions are allowed to be exported in a 'use server' file").
// → 상수만 이 플레인 모듈로 뗀다. 소비처(조판 표면·오케스트레이터)는 여기서
//   import 하고, 서버 액션은 worksheet-docs.ts 에서 import 한다.

/**
 * 조판 요청 1회에 담을 수 있는 학습지 문서 수 — **배치 상한**이지 조판 상한이 아니다
 * (docs/class-studio-spec.md §3.10.22 E22-0 확정 계약 3).
 *
 * ── 구 `SHEET_COMPOSE_MAX_DOCS = 6` 폐기 근거(26-08-18 감독 실측) ────────────
 * 구 상수는 「`PassageReport.pages` JSON 이 문서당 수 MB 급」이라는 **추정**을
 * 근거로 문서 축에 하드 상한 6을 걸고, 초과를 명시 실패로 끝냈다. 같은 주석이
 * "무근거 상향 금지 — **실측이 먼저다**" 라는 해제 조건을 스스로 달아 두었고,
 * 그 조건이 아래 실측으로 충족됐다:
 *
 *   · 페이로드(실DB 40건): pages JSON **중앙값 49KB** · 최대 458KB ·
 *     40건 **합계 1.9MB** — 「문서당 수 MB」는 두 자릿수 배로 틀린 추정이었다.
 *   · 렌더(조판 아이템 30 → 300): 토글 **890ms → 883ms** — 곡선이 평평하다
 *     (아이템당 약 1.3ms, 문서당 힙 +7MB). 초선형 붕괴가 없다.
 *
 * → 문서 축 하드 상한은 폐기한다. 대신 3층으로 대체한다:
 *   ① 서버 **요청 배치 상한**(이 상수) — 클라가 청크해서 보내므로 정상 경로는
 *     절대 걸리지 않는다. 악의적/버그성 대량 id 배열만 여기서 접힌다.
 *   ② 클라 로더의 청크 로딩(조판 표면 소관).
 *   ③ 콘텐츠 축 **소프트 경고**(총 아이템 600 / A4 100p 초과 시 「무거워질 수
 *     있습니다」) — **막지 않는다**. 문서 개수는 애초에 무게의 대리 지표로도
 *     틀렸다(49KB 문서 8장 < 458KB 문서 1장).
 *
 * ⚠ 이 값을 낮추려면 위와 같은 급의 실측을 먼저 붙일 것. 그리고 이 상수를
 *   **선택 상한·표면 배너로 되돌리지 마라** — 사용자가 체크한 문서가 조용히
 *   빠지거나 「눌러도 안 바뀌는」 죽은 컨트롤이 되는 것이 곧 신뢰 사고다(E21-0).
 */
export const SHEET_COMPOSE_DOC_BATCH = 12;
