"use client";

// ============================================================================
// [DEPRECATED · 미사용] 구 "훈련" 탭 클라이언트.
//
// 학습 OS 재편으로 이 화면의 역할(유닛맵 + 누적 복합 세트)은 트랙 허브
// /g/track/grammar 로 이관됐다. /g/train 페이지는 이제 서버 리다이렉트만 하며
// 이 컴포넌트를 렌더하지 않는다(docs/study-os-spec.md §4.3).
//
// 파일은 이력 보존을 위해 남기되, 죽은 코드가 회귀를 부르지 않도록 스텁으로
// 축소했다. 구 구현은 커리큘럼 v1(파트 1~3, frequency:number) 을 전제해
// PART 0(b01~b07 · frequency:null)에서 깨진다 — 되살리지 말 것.
// 유닛맵이 필요하면 트랙 허브를 쓴다.
// ============================================================================

export function TrainClient() {
  return null;
}
