// build-builder-document 공개 API 배럴 — 경로보존(외부 importer 0수정).
export type { BuilderBlock, BuilderBlockType, BuilderHeader, BuilderItem, BuilderLayout, BuilderSettings } from "./model";
export { buildBuilderExamDocument } from "./assemble";
