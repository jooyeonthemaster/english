// ============================================================================
// 문장 전환(SENTENCE_TRANSFORM) md 프롬프트 — 정본(빈칸·어법) 7블록 골격의 이식본.
// 견본: prompts-antonym.ts / 계약 문서: docs/md-qgen-type-expansion-spec.md §1-B
//
// ⚠ 이 유형은 **서술형**이다. 선지가 없다 → `정답:` 줄도, `오답:` 블록도 없다.
//   정답의 유일 진실원은 `모범답안:` 줄 하나이고, 어댑터가 correctAnswer 로 복제한다
//   (question-schemas-essay.ts:78-86 · ESSAY_PROMPTS.SENTENCE_TRANSFORM:33 계약).
//
// 공예 서사는 question-prompts-essay.ts:29-46(SENTENCE_TRANSFORM) 의 실전 검증된
// 지시를 md 골격으로 옮긴 것이다 — 의미 보존 3대 검산(극성·hedge·의미역)은 실측
// 결함(전환하며 의미가 반전됨)에 대응해 다듬어진 이 유형의 심장이므로 그대로 산다.
// ============================================================================

import type { MdDifficulty, MdExplanationMode } from "./prompts";

/** 난이도별 조건 개수 — 게이트는 이 범위를 강제한다(프롬프트는 더 좁게 지시). */
export const SENTENCE_TRANSFORM_MD_CONDITION_RANGE: Record<
  MdDifficulty,
  { min: number; max: number }
> = {
  BASIC: { min: 1, max: 3 },
  INTERMEDIATE: { min: 1, max: 3 },
  // fast 정본의 killer-needs-multiple-conditions(dispatcher.ts:2541-2545, warning)를
  // md 게이트에서 반려로 승격한다 — md 는 qualityIssues 를 차단하지 않기 때문이다.
  KILLER: { min: 2, max: 4 },
};

/** 채점기준 항목 수 — 만점/0점 최소 2단계(fast 정본 "등급형 답안 기준" 계약). */
export const SENTENCE_TRANSFORM_MD_SCORING_MIN = 2;
export const SENTENCE_TRANSFORM_MD_SCORING_MAX = 5;

// few-shot 해부 — 규칙 나열보다 실물 해부가 공예를 끌어올린다는 정본의 확정 결론.
const SENTENCE_TRANSFORM_FEWSHOT = `## 모범 설계 해부 — 이 수준을 재현하라 (예시의 표현을 복사하지는 마라)
지문: 도시 녹지가 열섬 현상을 완화한다는 환경 글.
- 원문장 선정: "Because the canopy intercepts sunlight before it reaches the pavement, the surface stays measurably cooler." — 종속절 + 주절, 인과 접속사, 측정 부사가 한 문장에 들어 있어 **전환할 손잡이가 세 개**다. 반면 "This is important." 같은 문장은 손잡이가 없어 전환 자체가 성립하지 않는다.
- 조건 설계: ① "분사구문으로 전환할 것" ② "'Intercepting'으로 시작할 것". 두 번째 조건이 첫 번째의 **출력 형태를 하나로 고정**한다 — 이게 채점 폭주를 막는 장치다.
- 모범답안: "Intercepting sunlight before it reaches the pavement, the canopy keeps the surface measurably cooler."
- 의미 보존 검산: 극성 그대로(부정 없음) · hedge 없음(원문도 단정) · 의미역 그대로(가리는 주체는 여전히 canopy, 시원해지는 것은 여전히 surface). measurably 를 떨어뜨렸다면 "측정 가능할 만큼"이라는 정도 한정이 사라져 실패다.
- 이 설계가 아름다운 이유: 학생은 접속사를 지우고 동사를 -ing 로 바꾸는 것만으로는 답에 도달하지 못한다. 종속절의 주어(the canopy)를 주절로 끌어올려 주어를 통일해야 비로소 분사구문이 성립하기 때문이다 — 문법 지식이 아니라 **문장 구조를 읽는 힘**을 묻는다.`;

const SENTENCE_TRANSFORM_DESIGN_BY_DIFFICULTY: Record<MdDifficulty, string> = {
  BASIC: `## 전환 설계 (기본 난이도)
- 조건은 **1개**. 전환 축도 하나만 쓴다(태 전환 또는 시제 전환 또는 절 압축 중 택1).
- 원문장은 주어·동사가 한눈에 보이는 문장으로 고른다. 삽입절이 겹겹이 걸린 문장은 기본 난이도에 맞지 않는다.
- 🚫 **이미 그 형태인 문장 금지**: 수동태 문장에 "수동태로 바꿀 것"을 요구하면 답이 원문과 같아진다 — 그 순간 문항이 죽는다.`,
  INTERMEDIATE: `## 전환 설계 (중급 난이도)
- 조건은 **1~2개**. 축 하나(태·시제·절 압축·구문 전환 중 택1)에, 출력 형태를 좁히는 어휘 제약("'Not until'으로 시작할 것" 류)을 하나 얹어도 좋다.
- 원문장은 **종속절이 하나 이상 붙은 복문**을 우선한다. 단문은 전환해도 결과가 뻔해 변별이 안 난다.
- 전환 뒤에 주어가 바뀌는 자리(수동태의 행위자, 분사구문의 의미상 주어)를 학생이 스스로 맞춰야 하도록 설계하라 — 여기가 중급의 분기점이다.`,
  KILLER: `## 전환 설계 — KILLER 의 생명
- 조건은 **2개 이상**이고, 서로 **다른 축**이어야 한다(예: "가정법 과거완료로 + 도치할 것", "수동태로 + 분사구문으로"). 같은 축을 두 번 쪼개 쓴 것은 조건 2개가 아니다.
- 🚫 **1단계 기계적 이동만으로 풀리는 전환 금지**: 부사절을 문두로 옮기기, 접속사만 바꿔 끼우기 — 이런 것은 학생이 문장을 이해하지 않아도 손이 먼저 움직인다.
- 원문장은 **전환하면 다른 자리가 연쇄로 흔들리는 문장**을 골라라. 태를 바꾸면 시제 조동사가 따라 바뀌고, 절을 압축하면 주어를 통일해야 하는 문장이 이상적이다.
- 그러면서도 답은 **하나로 수렴**해야 한다. 조건 조합이 서로 다른 두 정답을 허용하면 킬러가 아니라 시비다.`,
};

function explanationBlock(mode: MdExplanationMode): string {
  const tail = `해설: <딱 2문장 — ①어떤 전환을 원문장의 어느 자리에 적용했는지 ②원문의 명제 의미(극성·hedge·의미역)가 어떻게 보존됐는지. 합니다체>`;
  if (mode === "answer-only") {
    return `${tail}\n(채점기준 섹션은 쓰지 마라.)`;
  }
  return `채점기준:
- 만점: <이 형태여야 만점이라는 서술 한 줄>
- 부분점수: <어순·축약 차이처럼 감점만 하고 인정하는 동치 변형 한 줄>
- 0점: <의미 변경·조건 미충족처럼 인정하지 않는 형태 한 줄>
${tail}`;
}

/**
 * 문장 전환 md 프롬프트.
 * 출력 계약(parser-sentence-transform.ts 와 1:1):
 *   `원문장:` 한 줄 + `조건:` 리스트 + `모범답안:` 한 줄 + `채점기준:` 리스트 + `해설:`.
 * 선지가 없으므로 `정답:` 줄도 `오답:` 블록도 없다 — 모범답안이 유일 진실원이다.
 */
export function buildMdSentenceTransformPrompt(
  passage: string,
  mode: MdExplanationMode = "full",
  difficulty: MdDifficulty = "KILLER",
): string {
  const range = SENTENCE_TRANSFORM_MD_CONDITION_RANGE[difficulty];
  const conditionScaffold =
    difficulty === "KILLER"
      ? `- <전환 조건 1 — 한국어 한 줄>
- <전환 조건 2 — 한국어 한 줄. KILLER 는 서로 다른 축 2개 이상 필수>`
      : `- <전환 조건 1 — 한국어 한 줄>
- <전환 조건 2 — 조건이 하나면 이 줄은 쓰지 마라>`;

  const headline =
    difficulty === "KILLER"
      ? `아래 지문에서 문장 하나를 골라 "주어진 조건에 맞게 바꾸어 쓰게" 하는 KILLER 서술형 문항 1개를 설계하라. 조건 하나하나에 명확한 출제 의도가 박힌 아름다운 킬러 문항이어야 한다. 학생이 어느 자리에서 손이 먼저 움직일지를 계산하고 학생 머리 꼭대기에서 설계하라.`
      : `아래 지문에서 문장 하나를 골라 "주어진 조건에 맞게 바꾸어 쓰게" 하는 서술형 문항 1개(난이도: ${difficulty === "BASIC" ? "기본 — 교과서 수준 확인형" : "중급 — 모의고사 중위권, 구조 판단 필요"})를 설계하라. 조건 하나하나에 명확한 출제 의도가 있어야 한다.`;

  const fewshotBlock = difficulty === "BASIC" ? "" : `${SENTENCE_TRANSFORM_FEWSHOT}\n\n`;

  return `너는 대한민국 내신 영어 서술형을 20년 출제해 온 최정상 출제위원이다. ${headline}

이 문항에는 **선지가 없다.** 학생은 빈 줄에 영어 문장을 직접 써낸다. 그래서 이 유형의 품질은
"고를 것을 잘 만들었는가"가 아니라 **"쓸 것이 하나로 정해지는가"** 로 갈린다.

${fewshotBlock}## 원문장 선정 — 여기서 문항의 격이 갈린다
- 원문장은 **아래 지문에 실제로 있는 문장**을 한 글자도 바꾸지 말고 그대로 옮긴다. 요약·의역·짜깁기는 실격이다(학생 시험지에는 그 문장이 지문 안에 밑줄로 표시되므로, 축자가 아니면 밑줄이 사라진다).
- 문장 **처음부터 마침표까지 통째로** 옮긴다. 절 하나만 잘라 오지 마라.
- **문장은 정확히 하나다.** 두 문장을 이어 붙이지 말고, 문단(빈 줄)을 넘어가지도 마라 — 시험지에서 밑줄이 걸리는 범위와 모범답안이 답하는 범위가 어긋나고, 지문을 동봉할 때 원문 뒷부분이 학생 지면에 그대로 인쇄된다.
- 지문에 두 번 이상 똑같이 나오는 문장은 고르지 마라 — 밑줄 자리가 유일하게 확정되지 않는다.
- **전환할 손잡이가 있는 문장**을 골라라: 종속절·분사구·수동태·조동사·비교급·인과 접속사 중 최소 하나. "This is why it matters." 처럼 손잡이가 없는 문장은 조건을 붙여도 전환이 성립하지 않는다.
- 너무 짧은 문장(6단어 미만)이나 지문 절반을 차지하는 문장(60단어 초과)은 피하라.

${SENTENCE_TRANSFORM_DESIGN_BY_DIFFICULTY[difficulty]}

## 전환 축 분류학 — 조건은 여기서 고른다 (같은 축을 두 번 쓰지 마라)
1. **태(voice)**: 능동↔수동. 행위자를 by 구로 남길지 지울지까지 조건이 정하게 하라.
2. **시제·상(tense/aspect)**: 단순↔완료·진행. 종속절 시제 일치가 따라 움직이는 문장에서만 의미가 있다.
3. **절 압축(reduction)**: 관계절↔분사구·동격구, 부사절↔분사구문. 의미상 주어 통일이 관건이다.
4. **구문 전환(construction)**: 가정법, 도치, it-cleft 강조, so~that↔too~to, not only A but also B, 비교구문.
5. **품사 전환(nominalization)**: 동사절↔명사구(that절↔동명사구·추상명사구).
6. **연결 전환(connective)**: 접속사↔전치사구(because↔because of, although↔despite, so↔therefore).

## 조건 작성 규칙 — 기계로 판정 가능해야 한다
- 조건은 **한국어 한 줄**이고, 채점자가 눈으로 O/X 를 그을 수 있어야 한다.
  좋은 예: "수동태로 바꿀 것" · "'Had'로 시작할 것" · "총 14단어로 쓸 것" · "'because'를 사용하지 말 것".
  나쁜 예: "자연스럽게 바꿀 것" · "더 세련되게 표현할 것" — 판정 불가라 채점이 무너진다.
- 필수·금지 어휘를 지정할 때는 **반드시 작은따옴표로 감싸라**('Without' 처럼). 따옴표가 없으면 기계 검사가 그 조건을 읽지 못한다.
- "총 N단어로 쓸 것"을 쓸 거면 **모범답안의 단어를 실제로 하나씩 세어** N과 맞춰라(하이픈 결합어는 1단어). 세지 않고 쓴 숫자는 그 자리에서 문항을 무효로 만든다.
- 단어 수가 **문장 전체**에 걸리는 조건이면 반드시 **'총'을 붙여라**("총 14단어로 쓸 것"). '총' 없이 "14단어로 쓸 것"이라고만 쓰면 기계 검사가 문장 일부를 가리키는 조건("전치사구는 4단어로 만들 것")과 구분하지 못해 세지 않는다.
- 조건끼리 모순되면 안 된다. 두 조건을 동시에 만족하는 문장이 실제로 써지는지 직접 써 보고 확인하라.

## 마감 — 위반하면 문항이 죽는다
- **모범답안은 원문장과 반드시 달라야 한다.** 구두점·대소문자만 다른 답은 "전환하지 않은 것"이며 즉시 무효다.
- **모범답안이 지문 어딘가에 그대로 있으면 안 된다.** 이 유형은 지문이 문제와 함께 학생에게 보인다 — 답이 지문에 있으면 베껴 쓰기 과제가 된다.
- 모범답안은 **완성된 영어 문장 한 줄**이다. 문장 중간을 자른 절단형·한국어 혼입·설명 덧붙임 전부 금지.
- **대안 답안을 덧붙이지 마라.** "Alternatively …" 같은 두 번째 문장을 쓰는 순간, 이 유형은 기계 채점이 없어 강사가 그 오염된 문장을 채점 기준으로 읽게 된다. 답은 문장 하나다.
- 조건을 전부 적용한 결과가 **부자연스러운 영어**면 조건 설계가 틀린 것이다. 조건을 고쳐라.

## 의미 보존 3대 검산 (이 유형의 심장 — 출력 전 반드시 대조하라)
원문장과 모범답안을 나란히 놓고 셋을 대조한다. 하나라도 어긋나면 전환 설계를 폐기하고 다시 만든다.
① **극성**: 긍정/부정이 뒤집히지 않았는가.
② **양상(hedge) 보존**: 원문의 seem·appear·may·might·tend to·usually 같은 완화 표현이 사라져 단정문이 되지 않았는가. (완화 표현을 떨어뜨리면 잠정적 주장이 확정 주장으로 바뀐다 — 의미 변경이다.)
③ **의미역 보존**: 누가 무엇을 하는가가 그대로인가. 태 전환은 허용되지만 행위자·대상 자체가 바뀌면 실패다.

## 출력 전 자기검산 (사고 안에서 수행)
- 원문장을 지문에서 **찾아 문자 단위로 대조**하라 — 한 글자라도 다르면 그대로 옮겨 적어라.
- 원문장이 지문에 정확히 한 번만 나오는지 확인하라.
- 조건을 하나씩 짚으며 "모범답안의 어느 부분이 이 조건을 충족하는가"를 답해보라 — 못 대는 조건은 삭제하거나 답을 고쳐라.
- 단어 수 조건을 썼다면 모범답안의 단어를 **실제로 세어** 확인하라.
- 원문장과 모범답안에서 구두점과 대소문자를 걷어냈을 때 두 문장이 같아지지 않는지 확인하라.
- 모범답안 문장이 지문 안에 통째로 들어 있지 않은지 확인하라.
- 의미 보존 3대 검산(극성·hedge·의미역)을 한 줄씩 답해보라.
- 해설은 한국어만 쓴다(영단어를 한국어 문장의 어휘로 섞지 마라 — 지문·모범답안 표현 인용만 허용).

## 출력 규칙 (기계 파싱 계약)
- 각 항목은 **한 줄에 하나**다. 한 줄에 두 정보를 담지 마라.
- 원문장·모범답안·해설은 각각 **줄바꿈 없이 한 줄**이다. 길어져도 줄을 나누지 마라 — 특히 해설이 인용한 영어 조각을 다음 줄로 밀어내지 마라.
- 조건과 채점기준은 \`- \` 로 시작하는 목록 **하나뿐**이다. 표·번호·파이프를 쓰지 마라.
- 아래 형식 밖의 문장(머리말·맺음말·"다음은 …입니다")을 붙이지 마라.

## 출력 형식 (마크다운 — 이 형식 그대로, 다른 말 붙이지 마라)
원문장: <지문에 있는 문장 하나를 축자 그대로. 한 줄.>
조건:
${conditionScaffold}
모범답안: <조건을 전부 적용한 영어 완성 문장 한 줄. 원문장과 반드시 다르다.>
${explanationBlock(mode)}

(조건은 ${range.min}~${range.max}개, 채점기준은 ${SENTENCE_TRANSFORM_MD_SCORING_MIN}~${SENTENCE_TRANSFORM_MD_SCORING_MAX}개로 쓴다.)

## 지문
${passage}`;
}
