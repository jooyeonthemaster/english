// ============================================================================
// 배열 영작(WORD_ORDER) md 프롬프트 — 정본(빈칸·어법) 7블록 골격의 이식본.
// 견본: prompts-antonym.ts · 계약 문서: docs/md-qgen-type-expansion-spec.md §1-B
//
// ⚠ 이 유형은 **서술형**이다 — 5지선다가 아니다. 선지가 없으므로
//   `정답: <①~⑤>` 줄도 `오답:` 블록도 존재하지 않는다. 정답의 유일 진실원은
//   `모범답안:` 줄이고, **그 줄 안의 ` / ` 청크 경계가 곧 정답 칩이다.**
//
// ⚠ 26-07-26 형식 개정(§1-B 철칙1) — 종전에는 정답 문장을 `모범답안:` 과 `칩:`
//   두 줄에 걸쳐 축자로 두 번 받고, 그 중복이 어긋나는지를 게이트(재구성 회계)가
//   감시했다. **중복을 만들고 게이트로 막는 구조 자체가 규범이 금지한 패턴**이고,
//   실제로 관사 하나(`the`)만 흘려도 문항이 통째로 반려돼 이 축이 최대 반려
//   계통이 됐다. 이제 정답은 한 곳(`모범답안:`)에서만 받고 칩은 마커 분할로
//   파생한다 → 과부족 0 이 검사가 아니라 항등식이 되어 실패 모드가 사라졌다.
//   `미끼:` 줄만 남는데, 그건 다른 줄이 말하지 않는 유일 정보다(중복 아님).
//
// 공예 서사는 question-prompts-essay.ts:71-92(WORD_ORDER) 의 실전 검증된 지시를
// md 골격으로 옮긴 것이다 — verbatim 재배열 금지(실측 최다 결함)·미끼 필수 선언·
// 재구성 자체검산이 이미 실측 결함(runIndex 47/48 fatal)에 대응해 다듬어져 있다.
// ============================================================================

import type { MdDifficulty, MdExplanationMode } from "./prompts";

/** 정답을 쪼갤 청크 수 — 프롬프트가 요구하는 난이도별 목표치(게이트는 더 넓게 본다). */
export const WORD_ORDER_MD_ANSWER_CHUNK_TARGET: Record<
  MdDifficulty,
  { min: number; max: number }
> = {
  BASIC: { min: 4, max: 5 },
  INTERMEDIATE: { min: 5, max: 7 },
  KILLER: { min: 6, max: 8 },
};

/** 게이트 허용 범위 — 프롬프트 목표치보다 한 단계 넓다(정상 출력을 반려하지 않기 위해). */
export const WORD_ORDER_MD_ANSWER_CHUNK_MIN = 3;
export const WORD_ORDER_MD_ANSWER_CHUNK_MAX = 9;
export const WORD_ORDER_MD_CHIP_MIN = 4;
export const WORD_ORDER_MD_CHIP_MAX = 12;
/** 모범답안 최소 토큰 수 — 이보다 짧으면 배열 과제가 성립하지 않는다. */
export const WORD_ORDER_MD_ANSWER_TOKEN_MIN = 6;

/**
 * 미끼 최소 개수. fast 정본(question-prompts-essay.ts:83) 계약과 동일 —
 * "최소 1개(KILLER는 2개 이상)". 미끼가 0개면 "칩을 순서대로 전부 쓰기"가 되어
 * 함정 설계가 통째로 사라진다.
 */
export function wordOrderMdDistractorMin(difficulty: MdDifficulty): number {
  return difficulty === "KILLER" ? 2 : 1;
}

// few-shot 해부 — 규칙 나열보다 실물 해부가 공예를 끌어올린다는 정본의 확정 결론.
// ⚠ 이 해부에서 **값을 따옴표로 감싸 보이지 마라.** `라벨: "값"` 형태를 시연하면
//   모델이 실제 출력 줄에서도 값을 따옴표로 감싼다 — 청크로 쪼갠 뒤에는 여는
//   따옴표가 1번 청크, 닫는 따옴표가 N번 청크로 흩어져 파서가 짝을 못 찾고, 그
//   따옴표가 그대로 채점 correctAnswer 에 실려 정답을 맞힌 학생 전원이 오답이 됐다.
const WORD_ORDER_FEWSHOT = `## 모범 설계 해부 — 이 수준을 재현하라 (예시의 표현을 복사하지는 마라)
지문 문장 → Because the technology spread faster than regulators could respond, early adopters shaped the norms.
- 변형 설계: 원인절을 분사구문으로 접고 주절을 수동으로 돌렸다 →
  모범답안 → Spreading faster than regulators could respond, the norms were shaped by early adopters.
  원문과 연속으로 겹치는 구간은 faster than regulators could respond 다섯 단어뿐이라 지문을 찾아 베끼는 것으로는 풀리지 않는다.
- 청크 설계: 정답 줄에 경계를 그어 \`Spreading faster / than regulators could respond, / the norms / were shaped / by early adopters.\`
  다섯 조각 전부 의미 단위이고, 어느 하나도 정답의 절반을 담지 않으며, 구두점만 남은 조각이 없다(쉼표는 앞 조각에 붙였다).
- 미끼 설계: "shaped"(태 축) 와 "by regulators"(행위자 축). 앞은 학생이 수동을 확정하지 못하면 집어넣고 싶어지고, 뒤는 누가 행위자인지 지문에서 확인하지 않으면 그럴듯하다. 둘 다 정답 칩과 길이·형식이 비슷해 "안 쓰는 칩"이라는 티가 나지 않는다.
- 이 설계가 아름다운 이유: 학생은 칩을 늘어놓기 전에 "누가 무엇을 했는가"와 "어느 절이 종속절인가"를 지문에서 먼저 확정해야 한다. 미끼 두 개가 정확히 그 두 판단을 시험하고, 형태만 훑어서는 어느 쪽도 배제되지 않는다.`;

const WORD_ORDER_TARGET_BY_DIFFICULTY: Record<MdDifficulty, string> = {
  BASIC: `## 표적 설계 (기본 난이도)
- 변형은 **한 가지만** 건다: 능동↔수동 또는 시제 전환. 그것만으로도 원문 어순과 눈에 띄게 달라진다.
- 청크는 2~4단어짜리 **의미 덩어리**로 4~5개. 전치사구·동사구를 통째로 묶어 주어 조립 부담을 낮춘다.
- 미끼는 **1개**. 정답 칩의 활용형(-s / -ing / -ed)으로 만들어 "형태만 다른 한 쌍"을 세운다.
- 힌트는 문장이 글에서 하는 역할을 한국어 한 줄로 준다(정답 직역은 금지).`,
  INTERMEDIATE: `## 표적 설계 (중급 난이도)
- 변형은 구문 전환을 **최소 1개** 건다: 관계절↔분사구문, 도치, 강조구문, not only A but also B, 가정법 중에서 고른다.
- 청크는 단어~짧은 구 단위로 5~7개. 관사·전치사를 뒤 명사에서 떼어 독립 칩으로 두면 판단 지점이 늘어난다.
- 미끼는 **1~2개**. 하나는 활용형(형태 축), 하나는 근접 의미어(어휘 축)로 축을 분산하라 — 같은 축의 미끼 둘은 한 번의 판단으로 함께 배제된다.
- 힌트는 논지에서의 역할만 가리킨다(예: 앞 문장과 대조를 이루는 결론).`,
  KILLER: `## 표적 설계 — KILLER 의 생명
- 변형 **2개 이상을 결합**한다(수동 + 분사구문, 가정법 + 도치, 관계절 해체 + 명사구 전환 …). 한 번의 기계적 이동으로 원문이 복원되면 실패다.
- 청크는 단어 단위로 6~8개. 절 경계가 여러 곳에 놓이도록 쪼개 어순 후보가 실제로 갈리게 하라.
- 미끼 **2개 이상**. 반드시 축을 분산한다: (1)활용형(-ing/-ed/3인칭) (2)행위자·전치사 뒤바꿈 (3)연결사·관계사 후보 (4)근접 의미어 — 이 중 서로 다른 둘 이상.
- 힌트는 쓰지 않는 편이 가장 좋다. 쓰더라도 논지 수준(글 전체에서 이 문장의 자리)까지만.`,
};

/**
 * 배열 영작 md 프롬프트.
 * 출력 계약(parser-word-order.ts 와 1:1): `모범답안:`(청크 경계 ` / ` 포함) ·
 * `미끼:` · `힌트:`(선택) · `허용답:`(선택) · `해설:`.
 * ⚠ `칩:` 줄은 계약에서 제거됐다(§1-B 철칙1) — 파서는 구형 출력을 폴백으로
 *   흡수하지만 프롬프트는 절대 요구하지 않는다.
 *
 * @param mode 이 유형은 선지가 없어 `오답:` 블록 자체가 없다. 두 모드의 출력
 *   형식은 동일하며(정본 시그니처 정합용), answer-only 는 해설을 한 문장으로 줄인다.
 */
export function buildMdWordOrderPrompt(
  passage: string,
  mode: MdExplanationMode = "full",
  difficulty: MdDifficulty = "KILLER",
): string {
  const chunk = WORD_ORDER_MD_ANSWER_CHUNK_TARGET[difficulty];
  const distractorMin = wordOrderMdDistractorMin(difficulty);

  const headline =
    difficulty === "KILLER"
      ? `아래 지문으로 "주어진 단어를 올바른 순서로 배열해 문장을 완성하는" 서술형 KILLER 문항 1개를 설계하라. 칩 하나하나에 명확한 출제 의도가 박힌 아름다운 킬러 문항이어야 한다. 학생이 어느 칩에서 흔들릴지를 계산하고 학생 머리 꼭대기에서 설계하라.`
      : `아래 지문으로 "주어진 단어를 올바른 순서로 배열해 문장을 완성하는" 서술형 문항 1개(난이도: ${
          difficulty === "BASIC"
            ? "기본 — 교과서 수준 확인형"
            : "중급 — 모의고사 중위권, 구문 판단 필요"
        })를 설계하라. 칩 하나하나에 명확한 출제 의도가 있어야 한다.`;

  const fewshotBlock = difficulty === "BASIC" ? "" : `${WORD_ORDER_FEWSHOT}\n\n`;

  const explanationLine =
    mode === "answer-only"
      ? `해설: <딱 1문장 — 어떤 구문 전환을 적용했는지. 합니다체>`
      : `해설: <딱 2문장 — ①어떤 구문 전환을 적용했는지 ②그 어순이 왜 유일하게 성립하는지. 합니다체>`;

  return `너는 대한민국 내신 영어 서술형을 20년 출제해 온 최정상 출제위원이다. ${headline}

${fewshotBlock}## 이 유형의 뼈대 — 선지가 없다
- 학생에게 보이는 것은 **뒤섞인 칩 목록**과 (있다면) 한국어 힌트뿐이다. 고를 선지가 없으므로 정답은 오직 하나의 완성 문장(\`모범답안:\`)이다.
- 채점은 학생이 쓴 문장을 모범답안(및 허용답)과 **문자열로 대조**해 이루어진다. 그래서 모범답안은 반드시 자연스럽게 완결된 한 문장이어야 한다.
- **칩을 따로 나열하지 않는다.** 정답 문장을 \` / \` 로 끊어 쓰면 그 조각이 곧 정답 칩이다. 정답과 칩을 두 번 적지 않으므로 둘이 어긋날 일 자체가 없다. 학생에게 보일 때는 프로그램이 자동으로 섞고 미끼를 끼워 넣는다.

## 모범답안 설계 — 여기서 문항의 격이 갈린다
- 지문의 한 문장을 근거로 삼되, 스크램블 **전에** 반드시 실제 변형을 거쳐라: 구문 전환(관계절↔분사구문·도치·강조·비교) / 태 전환 / 시제 전환 / 의미 보존 패러프레이즈(핵심 콜로케이션 교체).
- 🚫 **지문 문장 통째 재배열 금지.** 지문은 문항 안에 함께 인쇄되므로, 모범답안이 지문 문장의 축자 복사(또는 앞뒤만 잘라낸 부분열)이면 학생이 찾아 베껴 쓰는 "필사 문제"가 되어 배열 과제가 무의미해진다.
- 문장 중간을 뚝 자른 절단형 금지 — 주어와 정동사를 갖춘 완결 문장으로 끝나야 한다.
- 지엽 세부(숫자·고유명사 나열)가 아니라 글의 논지를 지고 있는 문장을 고른다.

${WORD_ORDER_TARGET_BY_DIFFICULTY[difficulty]}

## 미끼 칩 기제 분류학 — 같은 기제를 두 번 쓰지 마라
1. **형태 함정**: 정답 칩과 같은 어근, 다른 굴절(reduce ↔ reducing / reduced / reduces). 시제·태·수일치를 확정해야만 배제된다.
2. **역할 뒤바꿈**: 행위자·대상을 맞바꾼 전치사구(by early adopters ↔ by regulators). 지문에서 누가 무엇을 했는지 확인해야 한다.
3. **연결 함정**: 절 관계를 흔드는 접속사·관계사 후보(because ↔ although ↔ which). 종속 관계를 확정해야 한다.
4. **근접 의미어**: 정답 어휘의 동의어·유의어(shaped ↔ formed). 콜로케이션 감각을 시험한다.
- 🚫 지문에 없는 개념을 수입한 무관 단어는 미끼가 아니라 장식이다 — 학생이 읽지도 않고 버린다. 미끼 재료는 전부 이 지문의 소재·어휘에서 가져와라.
- 미끼 칩은 정답 칩과 **길이·형식이 비슷**해야 한다. 유독 짧거나 유독 긴 칩 하나가 "안 쓰는 칩"임을 흘리면 안 된다.

## 마감 — 위반하면 시험 요령으로 뚫린다
- 모범답안과 지문 문장이 **연속 6단어 이상** 겹치면 그 문항은 무효다. 겹치면 변형을 더 넣어 다시 설계하라.
- 한 칩이 정답의 **절반 이상**을 담으면 배열 과제가 사라진다. 청크는 ${chunk.min}~${chunk.max}개로 고르게 쪼갠다.
- 구두점만 남은 조각(\`,\` \`.\` \`and,\`)을 만들지 마라. 쉼표·마침표는 **앞 조각 끝에 붙여라**(\`than regulators could respond, / the norms\`).
- 미끼는 정답 청크와 **글자 하나까지 똑같으면 안 된다** — 같은 칩이 두 번 제시돼 함정이 아니라 잡음이 된다.
- 힌트를 정답의 1:1 직역으로 쓰지 마라 — 되옮기기만 하면 풀려 변별력이 사라진다.

## 출력 전 자기검산 (사고 안에서 수행)
- 모범답안과 지문의 대응 문장을 나란히 놓고 연속으로 겹치는 최장 구간의 단어 수를 세어라. 6단어 이상이면 변형을 더 넣어 다시 써라.
- \`모범답안:\` 줄에서 \` / \` 를 지우고 이어 읽어 보라 — 주어와 정동사를 갖춘 자연스러운 완결 문장이어야 한다.
- 그 줄의 \` / \` 개수를 세어 청크가 ${chunk.min}~${chunk.max}개인지 확인하라.
- 미끼 ${distractorMin}개 이상 각각에 대해 "학생이 이걸 왜 집어넣고 싶어지는가"를 한 줄로 답해 보라 — 못 대는 미끼는 무관 단어이므로 교체하라.
- 허용답을 쓰기로 했다면, 그 문장이 **정답 청크만으로 과부족 없이** 조립되는지 단어 단위로 세어 확인하라. 축약형(it's ↔ it is)·단어 치환·단어 추가/누락은 칩으로 만들 수 없으므로 절대 넣지 마라. 조금이라도 확신이 없으면 \`허용답:\` 섹션을 통째로 빼라(빼도 채점은 모범답안으로 정상 작동한다).
- 해설은 한국어만 쓴다(영단어를 한국어 문장의 어휘로 섞지 마라 — 정답·지문 표현 인용만 허용).

## 줄 작성 규칙 (기계 파싱 계약)
- 각 항목은 한 줄에 하나, \`라벨: 값\` 형태다. 라벨을 바꾸거나 새 라벨을 만들지 마라. \`칩:\` 줄은 **만들지 마라** — 청크는 \`모범답안:\` 줄에서 나온다.
- 🚫 **값을 감싸지 마라.** 라벨 뒤에는 맨 값만 온다 — 따옴표(\`"…"\`)·괄호·대괄호로 감싸거나 굵게(\`**\`)·기울임(\`_\`)·표 칸(\`|\`)을 붙이지 마라. 감싼 기호는 채점 문자열에 그대로 실려 **정답을 정확히 쓴 학생이 오답 처리**된다.
- 청크·미끼 구분자는 **양옆에 공백을 둔 \` / \`** 하나뿐이다. 쉼표·파이프·번호를 쓰지 마라(칩 안에 쉼표가 들어갈 수 있다).
- 단어 안의 슬래시(\`and/or\` \`km/h\`)는 구분자가 아니다 — **공백 없이 붙여 쓰면** 한 칩으로 보존된다.
- 허용답은 \`- \` 불릿으로 한 줄에 하나씩 쓴다.
- \`해설:\` 이 마지막 줄이다. 그 뒤에 설계 노트·발문·요약 같은 **어떤 줄도 덧붙이지 마라.**

## 출력 형식 (마크다운 — 이 형식 그대로, 다른 말 붙이지 마라)
모범답안: <완성된 영어 문장 한 줄을 청크 경계마다 \` / \` 로 끊어 쓴다. 마침표로 끝낸다. 예) Spreading faster / than regulators could respond, / the norms / were shaped / by early adopters.>
미끼: <미끼1 / 미끼2 — 정답에 쓰이지 않는 **새 칩**. 위 청크를 다시 적는 것이 아니다. ${distractorMin}개 이상>
힌트: <한국어 한 줄 — 정답 직역 금지. 쓰지 않으려면 이 줄을 통째로 빼라.>
허용답:
- <정답 청크만으로 과부족 없이 조립되는 등가 어순 문장. 확신 없으면 이 섹션을 통째로 빼라.>
${explanationLine}

## 지문
${passage}`;
}
