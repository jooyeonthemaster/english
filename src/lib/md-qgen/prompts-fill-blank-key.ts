// ============================================================================
// 핵심 표현 빈칸(FILL_BLANK_KEY) md 프롬프트 — 정본(빈칸·어법) 7블록 골격의 이식본.
// 견본: prompts-antonym.ts / 계약 문서: docs/md-qgen-type-expansion-spec.md
//
// ⚠ 이 유형은 **서술형**이다. 선지가 없고 학생이 표현을 직접 써 넣으며, 채점은
//   exam-scoring 의 EXACT 집합 대조(grade.ts:50 — normalizeText 후 완전일치)다.
//   그래서 5지선다 유형의 "오답 기제 분류학" 블록이 성립하지 않는다. 그 자리를
//   이 유형의 진짜 실패축인 **근접 오답 예상(채점 붕괴 방어)** 블록이 대신한다.
//
// 형식 설계(§1-B 철칙 대조 완료 — 받는 칸은 3개뿐):
//   빈칸문장 / 정답 / 해설.
//   · passageWithBlank  → 후처리(processFillBlankKey)가 전담. 받지 않는다.
//   · direction         → 어댑터 상수. 받지 않는다.
//   · correctAnswer     → = 정답. 받지 않는다(철칙1: 같은 사실을 두 번 받지 마라).
//   · acceptedAnswers   → **받지 않는다.** 이 유형의 허용답 계약은 스키마상
//     "표기 변형만"(question-schemas-essay.ts:96-98)인데, 표기 변형은 축약형·
//     대소문자·구두점뿐이라 전부 결정론으로 파생 가능하다(adapter-fill-blank-key.ts).
//     모델에게 받으면 얻는 것은 0이고, 잃는 것은 크다 — 모델이 동의어를 한 줄
//     끼워 넣는 순간 지문에 없는 표현을 쓴 학생이 만점을 받는다(되돌릴 수 없는
//     채점 사고). 칸을 없애 그 실패 모드를 **구조적으로 소멸**시켰다.
//     (드리프트로 `허용답:` 이 오면 파서가 읽고 스냅이 결정형으로 절삭한다 —
//      조용히 버리지 않기 위해 corrections 에 남긴다.)
// ============================================================================

import type { MdDifficulty, MdExplanationMode } from "./prompts";

/** 학생 표면 빈칸 마커 — 후처리 BLANK(question-postprocess/types.ts:27)와 동일. */
export const FILL_BLANK_KEY_MD_BLANK = "_____";

/** 정답 단어 수 게이트 폭. 프롬프트 권장은 2~5, 게이트는 드리프트 여유를 둔다. */
export const FILL_BLANK_KEY_MD_WORD_MIN = 2;
export const FILL_BLANK_KEY_MD_WORD_MAX = 7;

/**
 * 해설이 지문을 인용할 때 한 조각의 상한(단어). **프롬프트가 집행하는 순응 규칙**이고,
 * 게이트(gate-fill-blank-key.ts PASSAGE_ECHO_WORDS)는 여기에 여유를 두고 훨씬 뒤에 선다.
 * 이 상한이 없으면 "지문 축자 그대로 인용하라"는 지시만 남아, 20~30단어짜리 지문 문장을
 * 통째로 인용한 **정상 문항**이 재출력 게이트에 걸려 재생성 1회를 태운다(실측 절벽).
 */
export const FILL_BLANK_KEY_MD_QUOTE_WORD_MAX = 12;

// few-shot 해부 — 규칙 나열보다 실물 해부가 공예를 끌어올린다(정본의 확정 결론).
// 이 유형의 급소는 "정답의 유일성"이다. 그 유일성이 어디서 나오는지를 해부한다.
const FILL_BLANK_KEY_FEWSHOT = `## 모범 설계 해부 — 이 수준을 재현하라 (예시의 표현을 복사하지는 마라)
지문: 혼자 연구하는 사람은 자기 논증의 사각을 못 본다 → 동료의 반박이 그 사각을 드러낸다 → 그래서 과학은 개인의 총명함이 아니라 집단의 검증 절차라는 글.
- 표적 문장(중반, 논지가 수렴하는 자리): "What a lone researcher cannot supply for herself is the adversarial scrutiny that turns a private hunch into a public claim."
- 빈칸 설계: \`What a lone researcher cannot supply for herself is the _____ that turns a private hunch into a public claim.\` / 정답 = adversarial scrutiny
- 정답이 **유일하게 좁혀지는** 세 장치:
  1. 앞 두 문장이 "동료가 논증을 공격한다 / 그때 사각이 드러난다"를 이미 서술해 의미를 지목한다.
  2. 빈칸 뒤 관계절(\`that turns ... into a public claim\`)이 단수 명사구를 통사적으로 강제한다 — 동사구·절은 애초에 들어갈 수 없다.
  3. 관사 the 를 빈칸 **밖에** 남겼다. 학생이 관사를 넣을지 말지로 갈리지 않는다.
- 그런데 \`adversarial scrutiny\` 는 지문 어디에도 다시 나오지 않는다. 앞 문장들은 "colleagues attack the argument", "blind spots surface" 처럼 **다른 말**로 그 개념을 예비해 두었을 뿐이다.
- 이 설계가 아름다운 이유: **문맥이 정답을 유일하게 지목하면서도, 축자로는 지문 어디에도 없다.** 학생은 찾아 베낄 수 없고 읽어야만 쓸 수 있다. 이 두 조건이 동시에 성립하는 자리가 이 유형의 유일한 정답 자리다.`;

const FILL_BLANK_KEY_TARGET_BY_DIFFICULTY: Record<MdDifficulty, string> = {
  BASIC: `## 표적 설계 (기본 난이도)
- 빈칸은 글의 요지가 드러나는 문장에 뚫되, **바로 앞이나 바로 뒤 문장이 그 개념을 다른 말로 이미 풀어 놓은** 자리를 골라라(근거 깊이 1문장).
- 정답은 2~3단어의 명료한 콜로케이션. 추상 명사구보다 구체적인 동사구·명사구가 낫다.
- 통사 구속이 강한 자리를 우선하라 — 빈칸 뒤에 관계절·전치사구·to부정사가 따라와 품사와 수(數)가 고정되는 자리.
- 지문 첫 문장에는 뚫지 마라(앞 문맥이 없어 유일 지목이 성립하지 않는다).`,
  INTERMEDIATE: `## 표적 설계 (중급 난이도)
- 빈칸은 논지 연결부에 뚫어라 — 인과 또는 대조로 **서로 다른 두 문장을 이어야** 그 표현이 확정되게(근거 깊이 2문장). 빈칸 문장 하나만 읽고 채워지면 미달이다.
- 앞 문단이 사례·현상으로 예비해 둔 개념을 **처음으로 명명하는 자리**가 최고의 표적이다. 학생은 앞의 서술을 한 어구로 압축해야 한다.
- 정답은 3~4단어. 지문 첫 문장에는 뚫지 마라.`,
  KILLER: `## 표적 설계 — KILLER 의 생명
- 빈칸은 글의 논지가 수렴하는 자리(중반 이후의 주제문·인과의 귀결). 첫 문장에는 절대 뚫지 마라.
- 근거가 서로 다른 문장 **2개 이상에 흩어져** 있어 종합해야만 표현이 확정되는 자리를 골라라. 빈칸 바로 옆 문장이 정답을 거의 그대로 풀어 써 놓은 자리는 시시하다.
- 정답은 2~5단어의 추상 명사구 또는 핵심 동사구 — 글 전체가 그 한 어구로 압축되는 자리.
- ⚠ **유일 지목은 KILLER 에서도 타협 불가다.** 이 유형에서 어려워야 하는 것은 "찾기"이지 "맞히기"가 아니다. 여러 표현이 다 들어맞는 자리는 어려운 문항이 아니라 **고장 난 문항**이다 — 자동채점은 축자 일치이므로, 동의어를 쓴 상위권 학생이 0점을 받는다.
- 🚫 표적 금지: 숫자·고유명사·연도·단순 연결어(however/therefore)·장식 수식어. 지우고도 문장 의미가 멀쩡한 자리는 표적이 아니다.`,
};

/** 근접 오답 예상 — 5지선다의 "오답 기제 분류학"에 대응하는 이 유형의 방어 블록. */
const FILL_BLANK_KEY_NEAR_MISS = `## 근접 오답 예상 — 이 유형에서 채점을 지키는 유일한 장치
이 문항에는 선지가 없다. 학생은 표현을 **직접 써 넣고**, 채점기는 지문 축자 정답과의 일치만 본다(대소문자·문말 구두점만 관용). 그래서 오답을 설계하는 대신 **오답이 생길 여지를 없애는** 것이 설계다.
- 빈칸을 정한 뒤, 학생이 그 자리에 쓸 법한 표현을 **3개** 실제로 떠올려라.
  · 셋 다 의미가 어긋난다 → 좋은 표적이다. 문맥이 정답을 유일하게 지목하고 있다는 뜻이다.
  · 셋 중 하나라도 **정답과 의미가 같은데 표기가 다르다**(동의어·어순 변형·관계사 치환) → 그 자리는 채점이 무너진다. **표적을 재선정하라.**
- 특히 위험한 자리: 형용사 하나만 비운 자리(important/crucial/vital 이 다 맞는다), 일반 동사 하나만 비운 자리(shows/reveals/indicates), 도치·강조 없이 평범한 목적어 명사구를 비운 자리.
- 안전한 자리: 글이 반복해 온 핵심 개념이 그 문장에서만 특정한 어구로 결정(結晶)되는 자리, 앞뒤 문장이 대명사·지시어로 그 어구를 되받는 자리, 관용 콜로케이션이라 대체어가 없는 자리.`;

const FILL_BLANK_KEY_CLOSING = `## 마감 — 위반하면 문항이 무효가 된다
- \`빈칸문장\` 은 지문의 **한 문장**이다. 두 문장을 이어 붙이거나 문장 중간에서 자르지 마라. 개행 없이 한 줄로 써라.
- 빈칸(\`${FILL_BLANK_KEY_MD_BLANK}\`)은 **정확히 1개**다. 두 곳을 비우면 한 정답에 빈칸이 둘이 되어 채점이 갈린다.
- 빈칸을 제외한 나머지는 원문과 **한 글자도** 달라선 안 된다(철자·구두점·대소문자 포함). 빈칸을 뚫으면서 어형을 다듬거나 관사를 고치지 마라.
- **정답 표현은 지문 전체에서 정확히 1회만 등장해야 한다.** 다른 곳에 그대로 또 있으면 빈칸 처리 뒤에도 본문에 정답이 남아 학생이 찾아 베낀다.
- **관사·전치사는 빈칸 밖에 남겨라.** \`the _____\` 가 옳고 \`_____\`(= "the adversarial scrutiny")는 틀렸다. 학생이 관사를 넣을지 말지로 갈리면 채점이 무너진다.
- 정답에 따옴표·괄호·마침표를 포함하지 마라. 정답은 문장 부호 없이 어구만이다.
- 허용답·동의어 목록을 따로 쓰지 마라. 표기 변형(축약형·대소문자)은 시스템이 결정론으로 만든다.`;

function fillBlankKeyExplanationBlock(mode: MdExplanationMode): string {
  if (mode === "answer-only") {
    return `해설: <딱 1문장 — 정답을 지목하는 지문 단서만. 합니다체>`;
  }
  return `해설: <딱 2문장 — ①빈칸 문장이 글의 논지에서 하는 역할 ②정답을 유일하게 지목하는 지문 단서(어느 문장의 무엇). 합니다체>`;
}

/**
 * 핵심 표현 빈칸 md 프롬프트.
 * 출력 계약(parser-fill-blank-key.ts 와 1:1): `빈칸문장:` + `정답:` + `해설:` 3줄.
 */
export function buildMdFillBlankKeyPrompt(
  passage: string,
  mode: MdExplanationMode = "full",
  difficulty: MdDifficulty = "KILLER",
): string {
  const headline =
    difficulty === "KILLER"
      ? "아래 지문에서 글의 논지를 지고 있는 핵심 표현 하나를 비우고, 학생이 그 표현을 직접 써 넣게 하는 KILLER 서술형 문항 1개를 설계하라. 빈칸 자리 하나에 명확한 출제 의도가 박힌 아름다운 킬러 문항이어야 한다. 학생이 어느 지점에서 흔들릴지를 계산하고 학생 머리 꼭대기에서 설계하라."
      : `아래 지문에서 글의 논지를 지고 있는 핵심 표현 하나를 비우고, 학생이 그 표현을 직접 써 넣게 하는 서술형 문항 1개(난이도: ${
          difficulty === "BASIC"
            ? "기본 — 교과서 수준 확인형"
            : "중급 — 모의고사 중위권, 두 문장을 이어야 확정"
        })를 설계하라. 빈칸 자리에 명확한 출제 의도가 있어야 한다.`;

  const fewshotBlock = difficulty === "BASIC" ? "" : `${FILL_BLANK_KEY_FEWSHOT}\n\n`;

  const wordHint =
    difficulty === "BASIC" ? "2~3단어" : difficulty === "INTERMEDIATE" ? "3~4단어" : "2~5단어";

  return `너는 대한민국 고교 내신 영어 서술형을 20년 출제해 온 최정상 출제위원이다. ${headline}

${fewshotBlock}## 이 유형의 급소 — 먼저 이것부터 이해하라
학생 화면에는 **지문 전체가 빈칸 하나만 뚫린 채로** 보이고, 학생은 그 자리에 들어갈 표현을 손으로 쓴다. 채점은 지문 축자 정답과의 **완전일치**다(대소문자·문말 구두점만 관용). 따라서 좋은 표적은 두 조건을 **동시에** 만족해야 한다.
1. **유일 지목**: 지문의 문맥이 그 자리에 들어갈 표현을 한 가지로 좁혀 준다.
2. **축자 부재**: 그 표현이 지문의 다른 어디에도 그대로 다시 나오지 않는다.
1번이 깨지면 정답을 아는 학생이 오답 처리되고, 2번이 깨지면 지문을 안 읽은 학생이 베껴 쓴다. 이 두 축이 이 유형의 전부다.

${FILL_BLANK_KEY_TARGET_BY_DIFFICULTY[difficulty]}

${FILL_BLANK_KEY_NEAR_MISS}

${FILL_BLANK_KEY_CLOSING}

## 출력 전 자기검산 (사고 안에서 수행)
- 빈칸에 정답을 되끼운 문장을 지문의 그 문장과 나란히 놓고 **한 글자씩** 대조하라 — 다르면 프레임을 고친 것이고 기계 검사에서 즉시 반려된다.
- 정답 표현을 지문 전체에서 세어 보라. **정확히 1회**가 아니면 표적을 재선정하라.
- 학생이 쓸 법한 표현 3개를 떠올려, 정답과 의미가 같은데 표기만 다른 것이 하나라도 있으면 표적을 재선정하라.
- 빈칸 문장 하나만 보고 채워지는지 검사하라 — 채워지면 표적 재선정(기본 난이도 제외).
- 정답의 첫 단어가 관사(a/an/the)이거나 마지막 글자가 구두점이면 스팬을 다시 잡아라.
- 해설은 한국어만 쓴다(영단어를 한국어 문장의 어휘로 섞지 마라). 지문 표현을 인용할 때는 **지문 축자 그대로** 따옴표에 넣어라 — 지문에 없는 영어를 인용하면 반려된다.
- ⚠ 인용은 **한 조각 ${FILL_BLANK_KEY_MD_QUOTE_WORD_MAX}단어 이내**로 잘라 쓴다. 지문 문장을 통째로 옮겨 오지 마라 — 근거가 되는 최소 어구만 따옴표에 넣고 나머지는 한국어로 설명하라(해설은 전체 400자 이내다).

## 출력 형식 (마크다운 — 이 형식 그대로, 다른 말 붙이지 마라)
빈칸문장: <지문의 그 문장을 한 줄로 옮기되, 정답 스팬만 ${FILL_BLANK_KEY_MD_BLANK} 로 바꾼다. 나머지는 한 글자도 바꾸지 마라>
정답: <${FILL_BLANK_KEY_MD_BLANK} 자리에 원래 있던 지문 축자 표현 (${wordHint}, 관사·구두점 제외)>
${fillBlankKeyExplanationBlock(mode)}

## 지문
${passage}`;
}
