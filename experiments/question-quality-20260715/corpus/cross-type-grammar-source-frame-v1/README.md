# Cross-type grammar source frame v1

Status: **HARD BLOCK — INSUFFICIENT DISJOINT SUPPLY**

The frozen historical grammar audit produced 19 dual-lens passes from 52 rows. Its observed rate is not used directly. The exact one-sided 95% Clopper–Pearson lower bound is `0.2541665012621336`. At that conservative rate, a target of 38 dual-lens grammar-frame passes requires 186 candidates for at least 95% target-reach probability. The exact reach probability is `0.952857936152603`; 185 candidates reach only `0.9487435585809036`.

The immutable v3 `focus-grammar-killer` queue contains 307 rows. Cross-type blank supply v2 already bound its exact first 157 rows. Only the ordered 150-row remainder is content-hash disjoint, leaving a 36-row shortfall. The target is not reduced, and no replacement, top-up, reordering, or outcome-aware selection is permitted.

`source-frame-public.json` binds public metadata for all 150 available remainder rows solely as shortage evidence. Its private, git-ignored counterpart binds the same rows to source identifiers and passage text. No 186-row operational review frame exists, no review is activated, and all 150 rows remain unreviewed. The builder did not inspect or judge passage text.

`REVIEW-PROTOCOL.md` is frozen before any review. It may activate only in a new, fully bound frame with all 186 preregistered rows. Once activated, both reviewers must independently inspect the full census; sampling, sharing decisions, and reading the other reviewer's artifacts are prohibited.

Even a future dual-lens pass does not establish campaign authorization. Rights handling, a current history/exposure refresh, provider/cost controls, split balance, and sealed generation assignment remain separate holds. Campaign-eligible rows: **0**.

No model/API/network/database call or full-question generation was used to build this artifact.
