# Cross-type grammar source frame v2

Status: **BOUND REVIEW FRAME — NOT CAMPAIGN AUTHORIZATION**

This v2 tests a distinct, preregistered population from frozen v1. V1 remains immutable shortage evidence for the policy that removed all 157 blank-screening rows. V2 instead removes only the 38 passages operationally allocated to blank-v2 development, confirmation, or reserve, plus the full transitive exact/normalized/near-duplicate cluster around those memberships. Unselected blank screening candidates are not treated as operational exposure.

The blank artifact contributes only the 38 frozen content-hash memberships. It does not contribute reviewer verdicts, reasons, notes, passage content, split labels, metadata cells, or prospective item outcomes. Queue text is processed mechanically only by the mandated frozen collision-exclusion engine; it is never a positive inclusion or ranking feature and was not manually inspected. The public provenance block and verifier enforce this boundary. The collision implementation covers exact normalized hashes, markup-stripped equality, document-range overlap, sequential fragments, five-gram similarity, and long contiguous overlap.

The 38 membership seeds have no additional collision rows within the immutable 307-row v3 `focus-grammar-killer` queue. Therefore 269 rows remain. V2 binds exactly the first 186 of those rows in the inherited queue order; the remaining 83 are not a post-review replacement pool.

The frozen historical grammar audit produced 19 dual-lens passes from 52 rows. Its observed rate is not used directly. The exact one-sided 95% Clopper–Pearson lower bound is `0.2541665012621336`. At that conservative rate, a target of 38 dual-lens grammar-frame passes requires 186 candidates for at least 95% target-reach probability. The exact reach probability is `0.952857936152603`; 185 candidates reach only `0.9487435585809036`.

Both independent reviewers must inspect all 186 rows under `REVIEW-PROTOCOL.md`. The public artifact contains metadata and content hashes but no passage text, blank membership list, or private source identifiers. The private row map is git-ignored. The frame builder did not inspect or judge passage text.

Even 38 future dual-lens passes will not authorize generation. Rights handling, refreshed history/exposure, reviewer reconciliation, provider/retry/cost controls, and the sealed generation assignment remain separate holds. Campaign-eligible rows: **0**.

No model/API/network/database call, secret access, or full-question generation was used to build this artifact.
