# Cross-type blank source frame v2

Status: **BOUND SUPPLY — NOT CAMPAIGN AUTHORIZATION**

The original strict-blank frame produced only 24 dual-lens frame passes from 59 rows. Its observed pass rate is not used directly. The one-sided 95% Clopper–Pearson lower bound is `0.298659465008`. With a target of 38 dual-lens passes, 157 candidates are the smallest queue whose binomial reach probability is at least 95%; 156 remains below 95%.

This frame takes exactly the first 157 rows of the already frozen v3 `focus-grammar-killer` queue. That queue is used only as a passage-quality source pool: future reviewers must judge blank-inference suitability from scratch. The cut is made before those reviews, without passage-text inspection by the prompt-profile author, and cannot be reordered, topped up, or replaced after outcomes are known.

The 157 content hashes are disjoint from all 59 strict-blank-v1 hashes. Metadata cover four discourse classes, five topic classes, and three length bands. Metadata diversity does not establish semantic or stylistic suitability.

Every row remains blocked until:

1. two independent full-census reviews under `REVIEW-PROTOCOL.md` are frozen;
2. disagreements are adjudicated without generator/profile identity;
3. at least 38 dual-lens passes exist and a genre-balanced 6-development/20-confirmatory split plus 12 reserves is sealed;
4. history is refreshed immediately before queue authorization;
5. source-rights handling is separately documented.

The public artifact contains metadata and content hashes but no passage text or private source identifiers. The private map is git-ignored. No model/API/network/DB call or full-question generation was used to build this frame.
