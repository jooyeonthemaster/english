# 유입 분석 구축 + 관리자 지표 정합 수리 — 완료 보고 (26-09-17 ~ 09-18)

> 읽는 순서: 이 문서(무엇이 생겼고 무엇이 고쳐졌나) → `ops-checklist.md`(배포 전 사람이 할 일) → `analytics-spec.md`(정본 계약) → `admin-metrics-audit-2609.md`(감사 근거).
> 작업 방식: 필살기 — 정찰 4기 → 구현 15기 → 적대 검수 21기 + 중재 → 수정 18기. 감독(본체)이 토대·견본·공용 수리·게이트를 직접 수행.
> **아직 커밋·배포하지 않았다**(건별 승인 규칙).

---

## 1. 한 줄 결론

비로그인 방문자까지 포함한 **자체 유입 분석 시스템**(수집기 + 9개 관리자 화면 + 픽셀 관리 + 추적 링크)을 새로 만들고, 관리자 콘솔의 **잘못된 숫자 18종**(결제 대기가 매출에 합산되던 것을 포함)을 근거와 함께 고쳤다. 코드 131파일 17,292줄(신규) + 기존 40파일 수정(+2,362/−823).

---

## 2. 새로 생긴 것

### 2.1 수집기 (전 방문자)
| 구성 | 경로 | 핵심 |
|---|---|---|
| 클라이언트 트래커 | `src/components/analytics/site-analytics.tsx`, `src/lib/analytics/client.ts` | 루트 레이아웃 1회 마운트. 관리자 화면 제외. 페이지뷰·체류·스크롤·CTA 클릭·외부 링크·다운로드·커스텀 이벤트 |
| 수집 API | `src/app/api/collect/route.ts` + `src/lib/analytics/ingest.ts` | 세션·방문자 멱등 upsert, 봇 차단, 계정 연결, 전환 판정 |
| 유입 분류기 | `src/lib/analytics/classify.ts` | 12채널 — 검색(자연)·검색광고·SNS·SNS광고·동영상·블로그·카페·커뮤니티·메신저·AI 검색·이메일·디스플레이·외부사이트·직접. 네이버 블로그/카페, 카톡·인스타 인앱(referrer 없어도 UA 로 보정), ChatGPT·퍼플렉시티 등 AI 유입, 광고 클릭ID(gclid·NaPm·fbclid…) |
| 거부 수단 | `src/lib/analytics/consent.ts` | 거부 쿠키·GPC·저장소 차단 시 **수집·픽셀 전부 정지**(실측 검증) |
| 보유기간 집행 | `src/app/api/cron/analytics-retention/route.ts` | 매일 03:30 KST, 365일 초과 파기 + 연결 방문자 유입요약 익명화, 배치·기록·드라이런 |
| 신규 테이블 6 | `prisma/migrations-manual/20260917_analytics.sql`(+인덱스 `20260918_…`) | 방문자·세션·이벤트·전환·추적링크·링크클릭 (추가 전용, 기존 테이블 변경 0) |

### 2.2 관리자 화면 `/admin/analytics` (좌측 메뉴 「마케팅 → 유입 분석」)
| 탭 | 보는 것 |
|---|---|
| 개요 | 지금 접속자·방문자·방문·페이지뷰·이탈률·평균 체류·신규 비율·가입·매출 + 추이(이전 기간 겹침) + 상위 채널·소스·페이지 |
| 실시간 | 최근 5분 접속자, 분당 페이지뷰 30분, 활성 세션(현재 페이지·유입·기기·지역·체류), 지금 유입 소스, 최근 이벤트 피드 (10초 갱신, 탭 숨기면 정지 표시) |
| 유입 경로 | 채널·소스·참조 도메인·전체 referrer·UTM 캠페인/콘텐츠/키워드·인앱 브라우저·AI 유입·광고 클릭ID·추적 링크. 행 클릭 = 필터. 귀속 모델(최초/직전) 토글 |
| 페이지 | 인기·진입·종료 페이지, 평균 체류·스크롤, 영역 비중, 페이지 흐름(이전→선택→다음), 동적 ID 묶기 |
| 방문자·시간대 | 요일×시간 히트맵, 최근 90일 캘린더, 방문 많은 날 TOP10, 기기·브라우저·OS·인앱·화면·언어·국가·시도·도시, 신규/재방문, 로그인/비로그인 |
| 전환·가입 | 퍼널(방문자→가입화면→가입→첫 결제), 채널·소스별 가입·결제·매출, **가입 학원 전체 목록 + 각 학원의 최초/직전 유입·방문 수·가입까지 일수·첫 결제·누적 매출**, 커스텀 이벤트, 픽셀 전환 기록 |
| 방문 여정 | 세션 목록(검색·필터·페이지네이션) + 세션 상세 드로어(이벤트 타임라인·유입 상세·같은 방문자의 다른 세션·연결 학원) |
| 추적 링크 | `/go/<slug>` 링크 만들기(인스타 프로필·스토리·카톡 공유·네이버 블로그/카페·유튜브·전단 QR 프리셋), 클릭·유입 세션·가입·매출, UTM 직접 빌더 |
| 픽셀·검색엔진 | 픽셀 ID 입력(재배포 없이 적용), 수집기 상태, 사이트맵 섹션별 URL 수·robots·llms·RSS, 소유확인·IndexNow, **GSC 연결 진단(403 원인·조치)**·검색어/페이지 성과, 네이버 서치어드바이저 안내, 개인정보 고지 체크리스트, 운영 조치 목록 |

표에는 CSV 내보내기가 붙어 있고, 모든 탭이 기간·필터를 공유한다.

### 2.3 픽셀 (관리자에서 ID만 입력)
GA4 · Google 태그 관리자 · Google Ads 전환 · Meta 픽셀 · 네이버 애널리틱스 · 네이버 검색광고 전환 · 카카오 픽셀 · TikTok · Microsoft Clarity.
- 가입 완료·결제 완료를 서버가 판정해 각 픽셀 전환으로 발사(발사 확인 ack 포함).
- **로드 금지 범위**: 학생·학부모·과외 앱, 단어훈련(`/g`), 공유 토큰 링크(`/t`·`/r`·`/a`), 인증 토큰 경로, 쿠폰·프로모션. Clarity 는 원장·강사 화면에서도 로드하지 않는다(학생 이름·성적 녹화 방지).

### 2.4 다른 화면에 붙은 것
- `/admin` 대시보드: 「지금 접속·오늘 방문자·상위 채널」 미니카드, 「최근 가입 학원(7일)」
- 회원 상세: 「유입 경로」 카드(최초/직전 유입·방문 수 → 방문 기록 보기)
- 결제 상세 모달: 「이 학원 결제 이력」(총 결제·환불·첫/마지막 결제·진행 중·미완료 + 전체 목록) + **회원 상세 / 학원 상세 / 유입 경로 보기** 버튼 ← 사용자 요청 그대로
- `/privacy`: 행태정보·쿠키·외부 도구 고지 + **방문 분석 거부 토글**

---

## 3. 고친 관리자 숫자 (근거: `admin-metrics-audit-2609.md`, 결정: 스펙 §9.2·§14)

| 화면 | 증상 | 조치 |
|---|---|---|
| 결제 관리 | **「오늘 결제」가 상태 필터 없이 전 상태 합산**(결제창만 열고 이탈한 PENDING 51건·399만원 포함), 서버 로컬 자정 기준 | 결제 완료분만·KST·결제일 기준. 대기는 「진행 중(30~60분 내)」과 「미완료(이탈·만료)」로 분리 |
| 결제 관리 | 표 「결제금액」이 미결제 건도 결제액처럼 표시 | 「주문금액」으로 바꾸고 미결제는 흐리게, 일시는 KST 고정 |
| 전 화면 | 매출 정의가 3벌(대시보드·원가·BEP)로 갈리고 환불이 원 결제일 매출을 소급 삭제 | 단일 헬퍼 `src/lib/admin-revenue.ts` — 결제일 가산·환불일 차감·구독·무통장 수동지급 포함 |
| 대시보드 | 「전체 활성」이 학원 307곳 중 14곳(ACTIVE만) | 정지·해지 제외로 변경 |
| 대시보드 | 「크레딧 소진 임박」이 TRIAL 학원 전부 누락 / 「크레딧 소모」가 실패 자동환급 미차감 / 「입금 대기·미확인 입금」이 목록과 불일치 / 항상 빈 「체험 종료 임박」 | 각각 수리, 마지막은 「최근 가입 학원」으로 교체 |
| 원가 분석 | 만료된 TRIAL 을 넣어 **가짜 MRR**, 크레딧 사용량이 충전 환불 회수분까지 차감, 기능별 마진이 하드코딩 단가 | ACTIVE·기간 유효 구독만, 환불 범위 한정, 판매 단가 기준 명시 |
| BEP | 내부 특별 지급(1억 크레딧) 때문에 「적자 −5.3억」 | 내부·테스트 계정 제외 + 표기(§14 D16) |
| 회원 상세 | 「최근 30일 사용」 KST 이중 변환(−9시간), 「누적 사용」보다 큰 값, 거래 라벨 오표기(무료 지급이 「충전」) | 시간대·정의·라벨 정정, CSV·가입일 필터 KST |
| 추천·무통장 | 「지급 완료」가 APPROVED 누락, 입금 대기 100건 절단 무표시, 수동지급 끝난 주문 재연결 위험 | 수리 + 경고 배지(흐름 변경은 D3·D4 로 금지 — 표시만) |

**결제·충전·환불·무통장 매칭·웹훅 실행 코드는 한 줄도 바꾸지 않았다**(무회귀 렌즈가 확인). DB 데이터 변경 0.

---

## 4. 게이트 (전부 음성테스트로 계기 검증)

| 게이트 | 결과 | 음성테스트 |
|---|---|---|
| 타입 검사 `npx tsc --noEmit` | 0 오류(신규 파일 154개 포함) | 일부러 타입 오류 주입 → RED, 복구 → GREEN |
| 분류기·정화기 `scripts/analytics-gate-classify.ts` | **117/117** | 규칙 1개 삭제 → 2건 RED |
| 단위테스트 `npm run test:unit` | `analytics G2` 통과(CI 편입) | — (기존 실패 27건은 이번 작업과 무관한 코퍼스·논술 하네스) |
| 수집 E2E `scripts/analytics-gate-e2e.mjs` | **91/91** (인스타·카톡·네이버·UTM·AI·토큰 경로·봇·관리자 제외·전환) | ingest 결함 주입 → 3건 RED |
| 거부 수단 실측 | 허용 3건 전송 / 거부 쿠키 0 / GPC 0 / 화면 토글 후 0 | — |
| 화면 전수 캡처 32장(데스크톱·모바일) | 콘솔 오류 0, 페이지 오류 0 | 검수 21기가 캡처를 직접 열어 판정 |
| 행동 게이트(실제 클릭·이동) | 필터·탭·드로어·추적링크 생성→302→삭제·결제 모달→회원 상세 이동 | — |

---

## 5. 남은 것

### 5.1 사람이 해야 하는 일
`docs/analytics/ops-checklist.md` 참조. 배포 차단 항목: **합성 시드 청소**, **마이그레이션 먼저 적용**(이미 적용됨 — 재확인만), **방침 개정 사전 공지**.
그 외: Vercel `CRON_SECRET`·`NEXT_PUBLIC_SITE_URL`·`NEXT_PUBLIC_ANALYTICS_START_AT`, 픽셀 ID 입력, GA4 「브라우저 기록 기반 페이지 변경」 끄기, Meta 자동 고급매칭 끄기, GSC 서비스계정 권한 + Search Console API 사용 설정, 법무 검토 3건.

### 5.2 사용자 결정 대기
1. 내부·테스트 계정을 **매출**에서도 제외할지(현재는 BEP 만 제외, 매출은 포함·표기).
2. 무통장 재연결 **서버 가드**(409)를 넣을지(현재 경고 배지만 — 결제 흐름 파일을 건드려야 함).
3. 미해결 무통장 입금 대기 14건 693,000원(최고령 6/30) 고객 응대·정리 방침.
4. 방문자/세션 id 서버 서명(HMAC) 도입 여부.
5. 차기 기능 우선순위: 이벤트 속성 분해 심화·리텐션 코호트·기간 비교 열·주석(광고 집행일)·목표 정의.

### 5.3 보류된 수정 73건
검수는 200건을 지적했고 135건을 이번 라운드에 집행, 20건은 수정자가 근거를 들어 반박(기각), 73건은 소유권·범위 때문에 보류했다. 보류 목록은 `scratchpad/synth.json` 과 각 수정자 보고에 남아 있으며, 성격은 ① 다른 소유자 파일이라 못 건드린 자구·링크 통일 ② 400줄 초과 기존 파일 분리 ③ 계산 정의 변경(기능별 마진 원가 분모 등) ④ 결제 흐름 가드. 다음 라운드 후보다.

---

## 6. 이 작업 밖의 외부 사건 (기록)

26-09-17 22:46~22:53 KST 에 `credit_top_ups` 의 PENDING 전량이 CANCELLED/FAILED 로 일괄 변경됐다(`failureMessage` = 「결제창 이탈 — 결제가 진행되지 않아 자동으로 취소 처리했습니다」, `portoneStatus` READY/NOT_FOUND/FAILED, 56건 중 41건 `cancelledAt` NULL).

- 이 세션 에이전트 전원의 도구 호출을 전수 스캔 → `credit_top_ups` 쓰기 **0건**(전부 grep·SELECT).
- 저장소 코드·전체 git 이력·원격 브랜치 전부에 해당 문구 **없음**. 운영 최신 배포는 3일 전(본인 계정).
- 같은 시각 내부 계정 「네안데르이동주」가 운영에서 결제를 연달아 시도(22:08·22:09·22:23·22:46).
→ **공동 작업자의 로컬 환경(운영 DB 연결)에서 실행된 작업으로 추정**. 배포 전에 공동 작업자의 미푸시 변경과 충돌 여부를 확인할 것.

---

## 7. 병합·배포 완료 (26-09-18)

### 7.1 협업자 브랜치 통합
- 운영 배포본은 협업자(이동주) `origin/20260918dongju`(779cfe3f, 09-18 02:18 배포)였다. 우리 작업을 그 위에 병합했다(`23172b9c`).
- 충돌 18파일(전부 관리자 화면) — 파일 1개당 에이전트 1기, **양쪽 기능 보존** 원칙. 그쪽 UI 규약·자동 대사·ERP 피드·Notion 알림·사이드바 재편 전부 유지, 우리 지표 정의·유입 분석 전부 유지.
- 병합 중 추가 정합(검수 3렌즈 + 재이식 7유닛):
  - ERP 매출 피드 → `admin-revenue` 헬퍼로 전환(가짜 환불 시각 15건 제거, 전액취소 라벨 교정)
  - 대시보드 상세 팝업 매출 = 타일과 동일 정의(전 기간 일별 불일치 0)
  - 추천 「지급 완료」 GRANTED+APPROVED 재이식, 원가 「가짜 MRR」 표기 재이식
  - 결제 카드 타입 단일화 + 협업자 「충전 실패 검토」 필터 복원
  - 대시보드 위험 카드 배선(소진 임박 + 체험 종료 임박은 있을 때만), 액션 타일 hint 복원
  - 유입 분석 화면을 어드민 UI 규약(PageHeader·AdminTabs·FilterBar)으로 이관
  - **결제 대기 임계 통일**: 표시 60분 → 서버 자동 대사와 같은 30분, 단위테스트로 동기화 잠금
- 어젯밤 PENDING 51건 일괄 취소의 정체 = 협업자의 `stale-topup-reconcile`(30분 초과 PENDING 을 포트원에 조회해 정리, 관리자 결제 페이지 진입 시 실행). 정상 기능으로 확인하고 유지.

### 7.2 배포
- 게이트: tsc 0 · `npm run build` 성공(홈 정적 유지) · 분류기 117/117 · 단위테스트 analytics 2건 · 수집 E2E 91/91 · 캡처 36장.
- 배포 전 운영 DB 분석 테이블 **전량 정리**(합성 1,429행 + 로컬 검증 흔적) → 세션·이벤트·방문자 0행에서 시작.
- 푸시 `origin/20260822jooyeon` → 배포 `vercel --prod` → `nara-52t67lm2n`(빌드 4분, Ready).
- 운영 스모크: `/`·`/privacy`·`/resources`·`robots.txt`·`sitemap.xml` 200 · `/admin/analytics` 307→로그인 · `/api/collect` 204 · `/api/analytics/config` `{"pixels":{}}` · robots 에 `Disallow: /go/`.
- **실수집 검증**: 인스타그램 인앱 UA + `l.instagram.com` referrer + UTM 으로 운영 방문 → `organic_social/instagram/prod_smoke`, 기기 mobile·inApp instagram·지역 KR-11, 페이지뷰 2건 기록 확인 후 검증 데이터 삭제.

### 7.3 배포 후 남은 사람 작업
`ops-checklist.md` 의 B·C 절 — 픽셀 ID 입력(`/admin/analytics/setup`), GA4 「브라우저 기록 기반 페이지 변경」 끄기, Meta 자동 고급매칭 끄기, GSC 서비스계정 권한 + Search Console API 사용 설정, 개인정보처리방침 개정 사전 공지, 법무 검토 3건. `CRON_SECRET`·`NEXT_PUBLIC_SITE_URL` 은 운영에 이미 설정돼 있음(확인 완료).
