# CLAUDE.md

## 파일 크기 규칙 (500줄)

**한 파일은 500줄 이하를 유지한다.**

### 적용 범위

- **신규 파일**: 처음부터 500줄을 넘기지 않는다. 넘길 것 같으면 만들 때부터 쪼갠다.
- **수정하는 기존 파일**: 작업 후 500줄을 넘으면, 그 작업 범위 안에서 분리를 검토하고 사용자에게 제안한다.
- **손대지 않는 기존 파일**: 그냥 둔다. 리팩토링 목적만으로 파일을 열지 않는다.

즉 규칙은 **"내가 오늘 건드린 파일"에만 적용**된다. 이 리포에는 이미 500줄 초과 파일이 260개 넘게 있고, 이를 일괄 정리하는 것은 이 규칙의 목적이 아니다.

### 500줄을 넘겼을 때

절대 조용히 넘기지 말 것. 아래 중 하나를 한다.

1. **분리한다** — 분리 경계가 명확하고(아래 참고), 현재 작업 범위 안에서 안전하게 끝낼 수 있을 때.
2. **제안하고 승인을 받는다** — 분리가 현재 작업보다 커지거나, 여러 파일을 건드려야 할 때. 작업은 그대로 끝내고, 마지막에 "이 파일이 N줄이 됐다. 이렇게 쪼갤 수 있다"를 한 줄로 보고한다.
3. **넘어간다고 명시한다** — 쪼개면 오히려 나빠질 때(아래 예외). 이유를 한 줄 남긴다.

말없이 파일을 1000줄로 키우는 것만 금지다.

### 분리 경계 (좋은 순서대로)

- **자체적으로 렌더링되는 하위 컴포넌트** → `<원본이름>-parts/` 디렉터리로. (이 리포의 기존 관례: `exam-create-wizard-parts/`, `generation-config-panel-parts/`)
- **순수 함수 / 계산 로직** → `lib/` 또는 같은 폴더의 `<이름>-utils.ts`
- **타입·상수·스키마** → `<이름>-types.ts`
- **커스텀 훅** → `use-<이름>.ts`
- **서버 액션 / API 핸들러의 독립 단계** → `_lib/` 하위 모듈

### 예외 (쪼개지 않아도 되는 것)

- 자동 생성 파일 (`*.generated.ts`)
- 스타일 상수 테이블, 매핑 테이블, 프롬프트 텍스트 등 **선형 데이터 나열**
- 쪼개면 prop drilling이나 순환 import가 생기는 강결합 상태 로직

### 하지 말 것

- 줄 수만 줄이려고 의미 없는 파일로 토막내기 (`part1.tsx`, `part2.tsx`)
- 요청받지 않은 파일을 리팩토링 목적으로 열기
- 분리하면서 동작을 함께 바꾸기 — 분리는 **동작 보존**이 원칙이다
